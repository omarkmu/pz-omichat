# Configuration

OmiChat does not use the game's sandbox options for configuration.
Instead, a custom configuration menu can be accessed directly from the chat as an admin by clicking **Open settings** under **Admin options**.

The options are documented in the menu using tooltips.
